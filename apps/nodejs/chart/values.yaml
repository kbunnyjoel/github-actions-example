replicaCount: 1

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  name: nodejs-service
  type: ClusterIP
  port: 80
  targetPort: 3000

ingress:
  enabled: true
  host: nodejs.bunnycloud.xyz
